spring:
  application:
    name: ReserveBase

  datasource:
    url: jdbc:mysql://localhost:3306/reservebase?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&useUnicode=true&character_set_server=utf8mb4
    username: root
    password: zerobase
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

jwt:
  secret: emVyb2Jhc2Utc3VwZXItc2VjcmV0LWtleS0xMjM0NTY3ODkwIQ==

logging:
  level:
    org.hibernate.SQL: debug
